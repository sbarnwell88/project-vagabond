<div>
    <div class="container-post-views"> 
        <h3><%= @post.title %></h3>
        <p><%= @post.tip %></p>

        <div>
            <button class="btn"> <%= link_to  "edit tip", edit_city_post_path %> </button></br><br/>

            <%= form_for [@post.city, @post], method: :delete do |f| %> 
            <%= f.submit "Delete", class: "delete btn", data: {confirm: "Are you sure?"} %>
            <% end %>
        </div>
    </div>
</div>